import { defineConfig } from "astro/config";

export default defineConfig({
  // Für GitHub Pages später:
  // site: "https://DEIN-GITHUB-USERNAME.github.io/DEIN-REPO-NAME/",
  // base: "/DEIN-REPO-NAME/",
  output: "static"
});
